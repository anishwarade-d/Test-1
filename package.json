{
  "name": "test-1-e2e-poc",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright + GitHub Actions POC running real Gurukula admin tests",
  "scripts": {
    "test": "playwright test",
    "test:admin": "playwright test --project=admin",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "dotenv": "^16.5.0"
  }
}
