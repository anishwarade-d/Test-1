{
  "name": "test-1-e2e-poc",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright + GitHub Actions POC",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
